Soon:
- correct tooltip bug on bar charts
- landing page (add instructions)
- space between directors name in top ratings
- longest streak stat for the overview

Future features:
- custom pages for each component:
    - overview: none
    - ratings distribution: click on each bar to see every film of the given rating (grid in a popup?)
    - genres/directors/decade: click to go on a page with all (more than 10)
        - left side of the page: full bar chart
        - right side of the page: when a bar is clicked, all films of the corresponding bars are displayed in a grid
    - recently watched: click to go on a page with all watched movies in a grid
    - top ratings: add a "see more" button for each tops, popup with the full top
- letterboxd handling
- weekly, monthly, yearly reports
- when clicking on a film, popup with details of the film